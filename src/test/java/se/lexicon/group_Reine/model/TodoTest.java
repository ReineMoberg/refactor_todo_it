package se.lexicon.group_Reine.model;

import org.junit.Before;
import org.junit.Test;
import org.junit.Assert;


public class TodoTest {

    /*private Todo testTodo;          //create object from Todo class
    private Person testPerson1;     //create object from Person class

    //initialize objects constructors
    @Before
    public void setUp() {
        testPerson1 = new Person(730307,"Ken","Moberg");
        testTodo = new Todo(11,"Buy some things");
    }

    //testing methods
    @Test
    public void testGetTodoId() {
        int expectedId = 11;
        Assert.assertTrue(expectedId == testTodo.getTodoId());
    }

    @Test
    public void testGetDescription() {
        String expectedDescription = "Buy some things";
        Assert.assertTrue(expectedDescription.equalsIgnoreCase(testTodo.getDescription()));
    }

    @Test
    public void testSetDescription() {
        String expectedDescription = "Buy other things";
        testTodo.setDescription("Buy other things");
        Assert.assertTrue(expectedDescription.equalsIgnoreCase(testTodo.getDescription()));
    }

    @Test
    public void testSetGetDone() {
        boolean expectedDone = true;
        testTodo.setDone(true);
        Assert.assertTrue(expectedDone == testTodo.isDone());
    }

    //could not get the function assertEquals(object1,object2) to work.
    //did not find any info as to why
    @Test
    public void testSetGetAssignee() {
        testTodo.setAssignee(testPerson1);
        int expectedId = 730307;
        String expectedFirstName = "Ken";
        String expectedLastName = "Moberg";
        Assert.assertTrue(expectedId == testTodo.getAssignee().getPersonID() && expectedFirstName.equalsIgnoreCase(testTodo.getAssignee().getFirstName()) && expectedLastName.equalsIgnoreCase(testTodo.getAssignee().getLastName()));
    }*/

}

